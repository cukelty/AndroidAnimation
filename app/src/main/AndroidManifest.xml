<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tianyuan.animation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SampleActivity" />
        <activity android:name=".ui.AnimationListActivity" />
        <activity android:name=".ui.JumpActivity" />
        <activity android:name=".custom.CustomActivity" />
        <activity android:name=".ui.ViewAniActivity" />
        <activity android:name=".ui.View2Activity" />
        <activity android:name=".ui.propertyActivity"></activity>
    </application>

</manifest>